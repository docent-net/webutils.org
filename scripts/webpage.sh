# this script should be put in /etc/profile.d/webpage.sh
# it's goal is to create ENV variable that keeps path to the webpage and
# can be read by any deployment scripts

export WEBPAGE_PATH=/srv/www/webutils.org